	types of parameters 
	1. Path Parameter
	2. Query Parameter
	
Path Parameter
	Example:  	https://reqres.in/api/register
	Method we used pass Path parameter is below	
	Method-:	.pathParam("myuser", "users")
	
Query parameter
		in the below example everything proceded after? are called as Query parameter
		  ?delay=3
		  
Example:	https://reqres.in/api/users?delay=3	
To Pass Query Parameter we use below method
Method:		.queryParam("page", 2)

------------------------Log()--------------
Log is used to print the Response Header, Body and Cookies in the console.
we can print the whole response are the part of the response using below methods
log().all()
log().Header()
log().Cookies()
log().body() 

------------------------Cookies --------------
If we want to verify the response then we have to go till then() methods and have to use other methods. 
IF we want to check the response data,we have store response in Response type veriable. we sholuld stop after CURL methods(get,put,post,delete).

To get single cookie value we have to use 
getCookie()

To get multiple cookies value we have to use 
getCookies()

------------------------Headers--------------


